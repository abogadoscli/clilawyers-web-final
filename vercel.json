
{
  "buildCommand": "cd app && yarn build",
  "outputDirectory": "app/.next",
  "installCommand": "cd app && yarn install",
  "framework": "nextjs",
  "functions": {
    "app/api/**/*.ts": {
      "runtime": "nodejs18.x"
    }
  }
}
